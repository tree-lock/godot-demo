---
description: Godot 4.7 C# 项目约定，避免 GDScript 和 Godot 3 写法
alwaysApply: true
---

# Godot 4.7 C#

这是 Godot 4.7 的 **C#** 2D 项目（Jolt 物理）。脚本用 `.cs`，不要写 GDScript。

## API

- 类必须是 `partial class`，并继承节点类型（如 `CharacterBody2D`）
- 用 `[Export]`、`[Signal]`，不要 `@export` / `signal`
- 节点用 `GetNode<T>("Path")`，不要 `$Path`
- 信号用 C# 事件：`Button.Pressed += OnPressed;`
- 移动：改 `Velocity`，调用无参 `MoveAndSlide()`
- 异步用 `await ToSignal(...)`，不要 `yield`
- 不要用 Godot 3 API（`KinematicBody2D`、带参 `MoveAndSlide` 等）

## 文件

- 脚本文件 PascalCase，与类名一致（如 `Player.cs`）
- 不要手改 `.godot/`、`.import`、大段 TileMap 二进制数据
- `.csproj` 的 `Godot.NET.Sdk` 版本跟编辑器走，不要乱改
